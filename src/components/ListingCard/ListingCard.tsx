import { Flex } from "@chakra-ui/react";

const ListingCard: React.FC<{
  imgSrc: string;
  title: string;
  platform: string;
}> = ({ imgSrc, title, platform }) => {
  return <Flex></Flex>;
};

export default ListingCard;
